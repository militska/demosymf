{{ form_start(form) }}

    {{ form_errors(form) }}
    {{ form_row(form._token) }}

    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.ins_user_id) }}
    {{ form_row(form.ins_date) }}

    {{ form_row(form.submit, { 'label': 'add' }) }}


{{ form_end(form, {'render_rest': false}) }}


